process Test = {

    untracked PSet maxEvents = {untracked int32 input = 1}
    
    source = EmptySource {
	untracked uint32 firstRun = 1   
	untracked uint32 numberEventsInRun = 1
    }
    
    service = MessageLogger {
	untracked vstring destinations = { "cout" }
	untracked PSet cout = { untracked string threshold = "INFO" }
    }
    
        
    es_source = PoolDBESSource { 
	VPSet toGet = {{ string record = "SiStripSummaryRcd" string tag = "SiStripSummary_test1" }}	
	string connect = "oracle://devdb10/CMS_COND_STRIP"
	#
	string timetype = "runnumber" 
	untracked string BlobStreamerName="TBufferBlobStreamingService"
	PSet DBParameters = {
	    untracked string authenticationPath="/afs/cern.ch/cms/DB/conddb"
	    untracked int32 messageLevel=2
	}
    }
    
    module prod =  SiStripSummaryReader {
	untracked uint32   printDebug = 6
    }

    path p = {
	prod
    }

       module print = AsciiOutputModule {}
       endpath ep = { print }
}
